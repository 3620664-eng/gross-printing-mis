function openCustomer(id){let c=customers.find(x=>x.id===id),js=jobs.filter(j=>j.cid===id),ps=payments.filter(p=>js.some(j=>j.id===p.jid)),paid=ps.reduce((s,p)=>s+p.amount,0),out=js.reduce((s,j)=>s+balance(j),0);modal(c.name,`<div class="stats"><div class="stat"><span>Outstanding</span><strong>${money(out)}</strong></div><div class="stat"><span>Paid all time</span><strong class="money">${money(paid)}</strong></div><div class="stat"><span>Total jobs</span><strong>${js.length}</strong></div><div class="stat"><span>Open jobs</span><strong>${js.filter(j=>j.status!=='paid').length}</strong></div></div><div class="grid2"><div><div class="panel"><div class="panelHead"><h2>Jobs</h2></div><div class="panelBody">${js.map(jobLine).join('')}</div></div></div><div><div class="panel"><div class="panelHead"><h2>Full Payment History</h2></div><div class="panelBody">${ps.sort((a,b)=>b.date.localeCompare(a.date)).map(p=>{let j=jobs.find(x=>x.id===p.jid);return `<div class="payRow clickRow" onclick="openJob(${j.id})"><span>${p.date} · #${j.num}</span><strong class="money">${money(p.amount)}</strong></div>`}).join('')||'<div class="empty">No payments yet</div>'}</div></div></div></div>`,`<button class="btn danger" onclick="alert('TEST only: Admin Delete Customer with typed confirmation')">Delete Customer</button><button class="btn" onclick="closeModal()">Close</button>`)}
function openJob(id){let j=jobs.find(x=>x.id===id);modal(`Job #${j.num}`,`<div class="infoGrid"><div class="info"><small>Customer</small><strong>${cname(j)}</strong></div><div class="info"><small>Product</small><strong>${j.product}</strong></div><div class="info"><small>Status</small><strong>${label(j.status)}</strong></div><div class="info"><small>Created</small><strong>${j.created||'—'}</strong></div><div class="info"><small>Needed by</small><strong>${j.due}</strong></div><div class="info"><small>Quantity</small><strong>${j.qty}</strong></div><div class="info"><small>Size</small><strong>${j.size}</strong></div><div class="info"><small>Material</small><strong>${j.paper}</strong></div><div class="info"><small>Balance</small><strong>${money(balance(j))}</strong></div></div><h3 style="margin-top:18px">Job Files</h3><div class="files">${j.files.length?j.files.map(f=>fileRow(f)).join(''):'<div class="empty">No files yet.</div>'}</div><h3 style="margin-top:18px">Payment</h3><div class="infoGrid"><div class="info"><small>Price</small><strong>${money(j.price)}</strong></div><div class="info"><small>Paid</small><strong class="money">${money(j.paid)}</strong></div><div class="info"><small>Balance</small><strong>${money(balance(j))}</strong></div></div>`,`<button class="btn" onclick="openJobForm(${j.id})">Edit Job</button><button class="btn primary" onclick="alert('TEST payment entry screen')">Record Payment</button><button class="btn" onclick="closeModal()">Close</button>`)}
function fileRow(f,editable=false){return `<div class="file"><div class="fileName"><span class="fileType">${fileIcon(f)}</span><div><strong>${f}</strong><span class="muted">Stored in My Files</span></div></div><div class="fileBtns"><button class="btn small" onclick="previewFile('${f}')">View File</button><button class="btn small primary" onclick="openFile('${f}')">Open File</button>${editable?`<button class="btn small" type="button" onclick="alert('TEST: file removed from this job')">×</button>`:''}</div></div>`}
function fileIcon(name){let ext=name.split('.').pop().toLowerCase();return ext==='pdf'?'PDF':(['doc','docx'].includes(ext)?'W':'FILE')}
function allFiles(){return [...new Set(jobs.flatMap(j=>j.files))]}
function previewFile(name){let files=allFiles();$('#preview').innerHTML=`<div class="filePreview"><div class="myFilesCard"><aside class="myFilesSide"><div class="myFilesTitle">My Files</div><div class="muted" style="color:#b9c7d8;margin-bottom:12px">Gross Printing CRM</div>${files.map(f=>`<button class="myFileItem ${f===name?'active':''}" onclick="previewFile('${f}')"><span>${fileIcon(f)}</span><b>${f}</b></button>`).join('')}</aside><section class="myFilesMain"><div class="previewHead"><div><strong>My Files / ${name}</strong><div class="muted">View inside CRM</div></div><div><button class="btn small primary" onclick="openFile('${name}')">Open File</button> <button class="x" onclick="closePreview()">×</button></div></div><div class="paperPreview">${fileDocumentPreview(name)}</div></section></div></div>`}
function fileDocumentPreview(name){let ext=name.split('.').pop().toLowerCase();if(['doc','docx'].includes(ext))return `<div class="paper wordPaper"><div class="wordBar">W &nbsp; Microsoft Word document preview</div><h2 style="font-size:28px;margin-top:70px">${name}</h2><p>Sample Word file content for the TEST preview.</p><div class="note" style="margin-top:80px">In the live CRM, <b>View File</b> opens the file here in My Files.</div></div>`;return `<div class="paper"><div class="gp2">GP</div><h2>Gross Printing</h2><p>${name}</p><p>Sample PDF artwork / proof</p><div class="note" style="margin-top:80px">In the live CRM, <b>View File</b> opens the file here in My Files.</div></div>`}
function closePreview(){$('#preview').innerHTML=''}
function openFile(name){let ext=name.split('.').pop().toLowerCase(),kind=ext==='pdf'?'PDF / Acrobat-style viewer':(['doc','docx'].includes(ext)?'Microsoft Word':'Default file viewer'),w=open('','_blank');w.document.write(`<!doctype html><title>${name}</title><body style="margin:0;background:#45484d;font-family:Segoe UI,Arial"><div style="height:52px;background:#202327;color:#fff;display:flex;align-items:center;padding:0 18px;gap:14px"><b>${kind}</b><span style="opacity:.75">${name}</span></div><div style="width:760px;min-height:900px;background:white;margin:30px auto;padding:60px;box-sizing:border-box;box-shadow:0 5px 30px #0008;text-align:center"><div style="font-size:44px;color:#163f72;font-weight:900;margin-top:150px">${ext==='pdf'?'PDF':(['doc','docx'].includes(ext)?'WORD':'FILE')}</div><h2>${name}</h2><p style="color:#777">TEST fake file opened in a separate external-viewer style tab.</p><p style="color:#777">The live CRM will open the real file using its normal PDF / Word handling.</p></div></body>`);w.document.close()}
function openJobForm(id){let j=id?jobs.find(x=>x.id===id):null;modal(j?`Edit Job #${j.num}`:'New Job',`<div class="note">Job number and created date are automatic. Every dropdown can also accept your own typed value.</div><div class="formSection"><h3>1. Customer & price</h3><div class="formGrid"><label>Customer<select id="fc">${customers.map(c=>`<option value="${c.id}" ${j&&j.cid===c.id?'selected':''}>${c.name}</option>`).join('')}</select></label><label>Job name<input id="ft" value="${j?.title||''}" placeholder="Example: 5,000 #10 Envelopes"></label><label>Customer price<input id="fp" value="${j?.price||''}" placeholder="0.00"></label><label>Needed by<input type="date" id="fd" value="${j?.due||''}"></label></div></div><div class="formSection"><h3>2. Product & printing</h3><div class="formGrid"><label class="span2">Product type<input id="fprod" list="products" value="${j?.product||''}" oninput="smartFields()"><datalist id="products"><option>Flyer / Sheet</option><option>Booklet</option><option>Label / Sticker</option><option>Sign / Board</option><option>Banner</option><option>Blueprint / Plan</option><option>Custom</option></datalist></label><label>Quantity<input list="qtys" value="${j?.qty||''}" placeholder="Choose or type"><datalist id="qtys"><option>100</option><option>500</option><option>1,000</option><option>2,500</option><option>5,000</option></datalist></label><label>Size<input list="sizes" value="${j?.size||''}" placeholder="Choose or type"><datalist id="sizes"><option>8.5 × 11</option><option>11 × 17</option><option>24 × 36</option><option>48 × 96</option></datalist></label><label>Paper / Material<input value="${j?.paper||''}" placeholder="Choose or type material"></label><label>Color<input value="${j?.color||''}" placeholder="Choose or type"></label><label>Sides<input value="${j?.sides||''}" placeholder="Choose or type"></label><label id="bleedField">Bleed<input value="${j?.bleed||''}" placeholder="Choose or type"></label><label class="span2">Finishing<input value="${j?.finish||''}" placeholder="Choose or type finishing"></label></div></div><div class="formSection"><h3>3. Shop</h3><div class="formGrid"><label>Assigned to<input value="${j?.worker||''}" placeholder="Choose or type worker"></label><label>Delivery<input placeholder="Choose or type delivery method"></label></div></div><div class="formSection"><h3>4. Job files</h3>${j?.files?.length?`<div class="files">${j.files.map(f=>fileRow(f,true)).join('')}</div>`:''}<button class="btn" style="margin-top:8px" onclick="event.preventDefault();alert('TEST: normal Windows file chooser will open here in the live CRM')">Choose Files…</button></div>`,`<button class="btn" onclick="closeModal()">Cancel</button><button class="btn primary" onclick="closeModal();alert('TEST preview saved — no real data changed')">${j?'Save Changes':'Create Job'}</button>`);smartFields()}
function smartFields(){let v=($('#fprod')?.value||'').toLowerCase();let hide=v.includes('sign')||v.includes('board')||v.includes('banner')||v.includes('blueprint')||v.includes('plan');$('#bleedField')?.classList.toggle('hidden',hide)}
function openCustomerForm(){modal('New Customer',`<div class="formGrid"><label>Name<input placeholder="Customer name"></label><label>Company<input></label><label>Phone<input></label><label>Email<input></label></div>`,`<button class="btn" onclick="closeModal()">Cancel</button><button class="btn primary" onclick="closeModal();alert('TEST preview saved — no real data changed')">Add Customer</button>`)}
function modal(title,body,foot=''){ $('#modal').innerHTML=`<div class="modal" onclick="if(event.target===this)closeModal()"><div class="modalCard"><div class="modalHead"><h2>${title}</h2><button class="x" onclick="closeModal()">×</button></div><div class="modalBody">${body}</div>${foot?`<div class="modalFoot">${foot}</div>`:''}</div></div>`}function closeModal(){$('#modal').innerHTML=''}render();
